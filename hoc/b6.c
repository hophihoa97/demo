#include<stdio.h>
#include<string.h>
#include<stdlib.h>
 void decode(char*key,char*value,int size){
 	int length=0,i=0,j=0;
	char*pos1="\0",*pos2="\0";
	if((pos1=strstr(char*)getenv("QUERY_STRING"),key)!=NULL);
	{
		for(i=0;i<=strley(key);i++)
			pos1++;
		if((pos2=strstr(pos1,"&"))!=NULL)
			length+pos2-pos1;
		else length+strlen(pos1);
	}


					}
main(){




	}
